# CT-LIO Docker 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 CT-LIO 创建 Docker 环境，支持在容器内编译、通过 X11 + NVIDIA GPU 显示 RViz、播放 rosbag 示例。

**Architecture:** 单阶段 Dockerfile 基于 `ros:noetic-desktop`，构建时安装系统依赖、编译 Ceres 2.1.0、克隆 livox_ros_driver，用 Python 脚本 patch packages.cmake 的两处硬编码路径，最后 `catkin_make`。docker-compose.yml 配置 NVIDIA runtime + X11 socket 挂载。

**Tech Stack:** Docker, ROS Noetic, Ceres 2.1.0 (源码编译), livox_ros_driver, ros-noetic-libg2o, NVIDIA Container Toolkit, X11

---

### Task 1: 创建 docker/entrypoint.sh

**Files:**
- Create: `docker/entrypoint.sh`

- [ ] **Step 1: 创建目录并写 entrypoint.sh**

```bash
mkdir -p docker
```

文件内容 `docker/entrypoint.sh`：
```bash
#!/bin/bash
set -e
source /opt/ros/noetic/setup.bash
source /catkin_ws/devel/setup.bash
exec "$@"
```

- [ ] **Step 2: 验证文件内容**

Run: `cat docker/entrypoint.sh`
Expected:
```
#!/bin/bash
set -e
source /opt/ros/noetic/setup.bash
source /catkin_ws/devel/setup.bash
exec "$@"
```

- [ ] **Step 3: Commit**

```bash
git add docker/entrypoint.sh
git commit -m "docker: add entrypoint script"
```

---

### Task 2: 创建 docker/patch_cmake.py

**Files:**
- Create: `docker/patch_cmake.py`

该脚本在 Docker 构建时运行，替换 packages.cmake 中两处硬编码的开发者路径。用 Python 字符串替换而非 sed，避免 shell 对 cmake `${}` 变量的展开问题。

- [ ] **Step 1: 写 patch_cmake.py**

文件内容 `docker/patch_cmake.py`：
```python
cmake_path = '/catkin_ws/src/ct-lio/cmake/packages.cmake'

with open(cmake_path, 'r') as f:
    content = f.read()

content = content.replace(
    'find_package(Ceres 2 REQUIRED PATHS /home/zhaochengwei/workspace/3rdparty/ceres_210)',
    'find_package(Ceres 2 REQUIRED)'
)
content = content.replace(
    'set(G2O_INCLUDE_DIRS ${3RDPARTY_DIR}/g2o-20201223/include)',
    'set(G2O_INCLUDE_DIRS /opt/ros/noetic/include)'
)
content = content.replace(
    'set(G2O_LIBRARY_DIRS ${3RDPARTY_DIR}/g2o-20201223/lib)',
    'set(G2O_LIBRARY_DIRS /opt/ros/noetic/lib)'
)

with open(cmake_path, 'w') as f:
    f.write(content)

print('packages.cmake patched successfully')
```

- [ ] **Step 2: 验证替换字符串与源文件精确匹配**

Run: `grep -n "ceres_210\|g2o-20201223" cmake/packages.cmake`
Expected（3 行）：
```
63:find_package(Ceres 2 REQUIRED PATHS /home/zhaochengwei/workspace/3rdparty/ceres_210)
73:set(G2O_INCLUDE_DIRS ${3RDPARTY_DIR}/g2o-20201223/include)
74:set(G2O_LIBRARY_DIRS ${3RDPARTY_DIR}/g2o-20201223/lib)
```
注：与 patch_cmake.py 中的 3 个 `replace()` 调用一一对应。

- [ ] **Step 3: Commit**

```bash
git add docker/patch_cmake.py
git commit -m "docker: add cmake patch script"
```

---

### Task 3: 创建 .dockerignore

**Files:**
- Create: `.dockerignore`

排除不必要文件，加速构建上下文传输。

- [ ] **Step 1: 写 .dockerignore**

文件内容 `.dockerignore`：
```
.git
bags
build
devel
log
docs
```

- [ ] **Step 2: Commit**

```bash
git add .dockerignore
git commit -m "docker: add .dockerignore"
```

---

### Task 4: 创建 Dockerfile

**Files:**
- Create: `Dockerfile`

- [ ] **Step 1: 写 Dockerfile**

文件内容 `Dockerfile`：
```dockerfile
FROM ros:noetic-desktop

ENV DEBIAN_FRONTEND=noninteractive

# System dependencies
RUN apt-get update && apt-get install -y \
    build-essential cmake git python3 \
    libgoogle-glog-dev libgflags-dev \
    libsuitesparse-dev \
    libpcl-dev \
    libeigen3-dev \
    libopencv-dev \
    libyaml-cpp-dev \
    libboost-all-dev \
    ros-noetic-pcl-ros \
    ros-noetic-pcl-conversions \
    ros-noetic-eigen-conversions \
    ros-noetic-tf \
    ros-noetic-libg2o \
    && rm -rf /var/lib/apt/lists/*

# Build Ceres 2.1.0 from source
# (Ubuntu 20.04 apt only provides Ceres 1.14; project requires Ceres 2)
RUN cd /tmp && \
    git clone --depth 1 --branch 2.1.0 https://github.com/ceres-solver/ceres-solver.git && \
    mkdir ceres-build && cd ceres-build && \
    cmake ../ceres-solver \
        -DBUILD_TESTING=OFF \
        -DBUILD_EXAMPLES=OFF \
        -DBUILD_BENCHMARKS=OFF \
        -DCMAKE_BUILD_TYPE=Release && \
    make -j$(nproc) && make install && ldconfig && \
    rm -rf /tmp/ceres-solver /tmp/ceres-build

# Setup catkin workspace and clone livox_ros_driver
WORKDIR /catkin_ws
RUN mkdir -p src && \
    cd src && \
    git clone --depth 1 https://github.com/Livox-SDK/livox_ros_driver.git

# Copy ct-lio source
COPY . src/ct-lio/

# Patch packages.cmake (remove hardcoded developer paths)
RUN python3 src/ct-lio/docker/patch_cmake.py

# Build catkin workspace
RUN /bin/bash -c "source /opt/ros/noetic/setup.bash && \
    catkin_make -DCMAKE_BUILD_TYPE=Release -j$(nproc)"

# Persist ROS setup in shell
RUN echo "source /opt/ros/noetic/setup.bash" >> /root/.bashrc && \
    echo "source /catkin_ws/devel/setup.bash" >> /root/.bashrc

# Entrypoint
COPY docker/entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
CMD ["bash"]
```

- [ ] **Step 2: Commit**

```bash
git add Dockerfile
git commit -m "docker: add Dockerfile"
```

---

### Task 5: 创建 docker-compose.yml

**Files:**
- Create: `docker-compose.yml`

- [ ] **Step 1: 写 docker-compose.yml**

文件内容 `docker-compose.yml`：
```yaml
version: '3.8'

services:
  ct-lio:
    build: .
    image: ct-lio:latest
    runtime: nvidia
    environment:
      - DISPLAY=${DISPLAY}
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./bags:/bags:ro
    network_mode: host
    stdin_open: true
    tty: true
```

- [ ] **Step 2: 验证 YAML 语法**

Run: `docker compose config`
Expected: 输出解析后的配置，无报错

- [ ] **Step 3: 创建 bags 目录并加入 .gitignore**

Run:
```bash
mkdir -p bags
echo "bags/" >> .gitignore
```

- [ ] **Step 4: Commit**

```bash
git add docker-compose.yml .gitignore
git commit -m "docker: add docker-compose with NVIDIA + X11 support"
```

---

### Task 6: 构建镜像并验证

- [ ] **Step 1: 构建镜像**

Run: `docker compose build 2>&1 | tee /tmp/ct_lio_build.log`
Expected: 最后一行出现 `Successfully built` 或 `writing image sha256:`
Note: 首次构建约需 20-40 分钟（Ceres 2 编译耗时）

- [ ] **Step 2: 验证可执行文件存在**

Run: `docker compose run --rm ct-lio ls /catkin_ws/devel/lib/ct_lio/`
Expected: 输出包含 `ct_lio_eskf` 和 `ct_lio_eskf_rosbag`

- [ ] **Step 3: 验证 Ceres 版本**

Run: `docker compose run --rm ct-lio bash -c "dpkg -l | grep ceres || find /usr/local/lib -name 'libceres*' | head -3"`
Expected: 看到 `/usr/local/lib/libceres.so` 等文件（非 apt 安装，无 dpkg 记录）

- [ ] **Step 4: 验证 RViz 可启动（X11）**

Run:
```bash
xhost +local:docker
docker compose run --rm ct-lio rviz
```
Expected: RViz 窗口出现在主机屏幕，无 OpenGL 错误

---

### Task 7: 运行 rosbag 示例

前置：将 bag 文件放入 `bags/` 目录（从 README 链接下载）。

- [ ] **Step 1: 主机开放 X11 权限**

Run: `xhost +local:docker`
Expected: `non-network local connections being added to access control list`

- [ ] **Step 2: 启动容器**

Run: `docker compose run --rm ct-lio bash`
Expected: 进入容器 bash 提示符（路径 `/catkin_ws`）

- [ ] **Step 3: 容器内启动 roscore（后台）**

Run（容器内）: `roscore &`
Expected: 看到 `started core service [/rosout]`，返回提示符

- [ ] **Step 4: 容器内启动 ct-lio（后台）**

Run（容器内）: `roslaunch ct_lio run_eskf.launch &`
Expected: RViz 窗口出现在主机屏幕，等待点云数据

- [ ] **Step 5: 播放 rosbag**

Run（容器内）: `rosbag play /bags/<your_bag_file>.bag`
Expected: RViz 中点云和轨迹开始更新，终端显示 rosbag 播放进度

- [ ] **Step 6: 查看时间日志（可选）**

Run（容器内）: `python3 /catkin_ws/src/ct-lio/scripts/all_time.py`
Expected: 输出时间统计图表
